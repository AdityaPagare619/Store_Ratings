# Environment configuration for backend
PORT=4000
JWT_SECRET=supersecretdevkey_change_me
# Default local Postgres (customize as needed)
# Note: actual credentials read at runtime from .env
DATABASE_URL="postgresql://postgres:Aditya@localhost:5432/store_ratings?schema=public"